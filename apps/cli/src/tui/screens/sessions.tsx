/** Sessions screen — placeholder pending full implementation. */
import { Empty, Panel } from "../components";

/** The Sessions screen. */
export const SessionsScreen = () => (
  <Panel flexGrow={1} focused title="Sessions">
    <Empty label="Sessions screen — implementation in progress." />
  </Panel>
);
