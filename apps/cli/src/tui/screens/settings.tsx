/** Settings screen — placeholder pending full implementation. */
import { Empty, Panel } from "../components";

/** The Settings screen. */
export const SettingsScreen = () => (
  <Panel flexGrow={1} focused title="Settings">
    <Empty label="Settings screen — implementation in progress." />
  </Panel>
);
