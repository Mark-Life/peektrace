/** Memory screen — placeholder pending full implementation. */
import { Empty, Panel } from "../components";

/** The Memory screen. */
export const MemoryScreen = () => (
  <Panel flexGrow={1} focused title="Memory">
    <Empty label="Memory screen — implementation in progress." />
  </Panel>
);
